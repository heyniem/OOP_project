package entities.AnimatedEntities.Weapons.Bomb;

import javafx.scene.image.Image;

public class BombFlameSegment extends Bomb{
    public BombFlameSegment(int xUnit, int yUnit, Image img, int id) {
        super(xUnit, yUnit, img, id);
    }
}
